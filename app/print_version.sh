#!/usr/bin/env bash

function print_version() {
  echo -e "====================================================="
  echo -e "= Mason WordPress sync ${C_GRN}${SCRIPT_VERSION}${C_OFF}-${C_VERSION}${SCRIPT_VERSION_NAME}${C_OFF}"
  echo -e "====================================================="
}
